# Google Safe Browsing API v4 — free key (10,000 req/day)
# Setup: https://console.cloud.google.com → APIs & Services → Safe Browsing API → Create Credentials
#
# Run with key:
#   GOOGLE_SAFE_BROWSING_API_KEY=your_key_here pnpm dev:rep
#
# Or copy to .env and run with Node --env-file:
#   node --env-file .env --watch hono-domain-reputation-api-server.js

GOOGLE_SAFE_BROWSING_API_KEY=your_google_safe_browsing_api_key_here
